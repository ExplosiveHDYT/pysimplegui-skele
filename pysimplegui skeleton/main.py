#imports PySuperSimpleGUI
from PySuperSimpleGUI import *
#put GUI code here
GUI = [
    ] 

#seperates the backend and front end part of the GUI, and also allows some level of editing
window = Window("garbage OS", GUI, size=(400, 400), resizable=True)
event, values = window.read()

#Add backend stuff here
while True:


#adds an X button to close the tab
X_Button()

